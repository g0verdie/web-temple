<div class="directory-container">
    <h1>Member Directory</h1>
    <p class="subtitle">Connect with fellow members. <a href="/account/directory">Manage your own listing</a>.</p>

    <% if (typeof showNudge !== 'undefined' && showNudge) { %>
        <div class="directory-nudge" role="status">
            <p>You're not listed in the member directory yet. Add your listing so other members can find you.</p>
            <div class="directory-nudge-actions">
                <a href="/account/directory" class="btn-primary">Add my listing</a>
                <form method="POST" action="/directory/nudge/dismiss" class="nudge-dismiss">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="link-button">Dismiss</button>
                </form>
            </div>
        </div>
    <% } %>

    <div class="directory-filters">
        <form method="GET" action="/directory" role="search">
            <input type="text" name="search" placeholder="Search by name or interest..." value="<%= filters.search || '' %>" aria-label="Search members by name or interest">
            <button type="submit">Search</button>
        </form>
    </div>

    <div class="directory-results">
        <% if (profiles && profiles.length > 0) { %>
            <ul class="member-list">
                <% profiles.forEach(profile => { %>
                    <li class="member-card">
                        <a class="member-card-link" href="/directory/<%= encodeURIComponent(profile.user_id) %>" aria-label="View profile: <%= profile.first_name %> <%= profile.last_name %>">
                            <%- include('../partials/initials-avatar', { initials: profile.initials }) %>
                            <div class="member-details">
                                <h2><%= profile.first_name %> <%= profile.last_name %></h2>
                                <% if (profile.interests) { %>
                                    <p class="member-interests"><%= profile.interests %></p>
                                <% } %>
                            </div>
                        </a>
                    </li>
                <% }) %>
            </ul>
        <% } else if (filters.search) { %>
            <p class="directory-empty">No members match "<%= filters.search %>". Try a different name or interest.</p>
        <% } else { %>
            <p class="directory-empty">No members are listed in the directory yet. <a href="/account/directory">Be the first to add your listing.</a></p>
        <% } %>
    </div>

    <% if (totalPages > 1) { %>
        <nav class="pagination" aria-label="Directory pages">
            <% if (currentPage > 1) { %>
                <a href="/directory?page=<%= currentPage - 1 %>&search=<%= encodeURIComponent(filters.search || '') %>" rel="prev">Previous</a>
            <% } %>
            <span>Page <%= currentPage %> of <%= totalPages %></span>
            <% if (currentPage < totalPages) { %>
                <a href="/directory?page=<%= currentPage + 1 %>&search=<%= encodeURIComponent(filters.search || '') %>" rel="next">Next</a>
            <% } %>
        </nav>
    <% } %>
</div>
