<div class="container account-settings">
    <h1>My Directory Listing</h1>
    <p class="subtitle">Choose what to share with other temple members. You are not listed unless you opt in, and you control which contact details appear.</p>

    <section class="settings-section" aria-labelledby="directory-heading">
        <h2 id="directory-heading">Profile &amp; Visibility</h2>
        <form id="directoryForm" class="settings-form" data-initial-listed="<%= profile.listed ? 'true' : 'false' %>" novalidate>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">

            <div class="form-row">
                <label for="phone">Phone</label>
                <input type="tel" id="phone" name="phone" value="<%= profile.phone || '' %>" autocomplete="tel" maxlength="32">
            </div>

            <div class="form-row">
                <label for="address">Address</label>
                <input type="text" id="address" name="address" value="<%= profile.address || '' %>" autocomplete="street-address" maxlength="200">
            </div>

            <div class="form-row">
                <span class="form-label" id="birthday-label">Birthday</span>
                <div class="birthday-fields" id="birthdayFields" role="group" aria-labelledby="birthday-label" aria-describedby="birthday-hint" data-birthday="<%= profile.birthday || '' %>">
                    <select id="birthday_month" aria-label="Birth month">
                        <option value="">Month</option>
                        <% ['January','February','March','April','May','June','July','August','September','October','November','December'].forEach(function (name, i) { %><option value="<%= String(i + 1).padStart(2, '0') %>"><%= name %></option><% }); %>
                    </select>
                    <select id="birthday_day" aria-label="Birth day">
                        <option value="">Day</option>
                        <% for (let d = 1; d <= 31; d++) { %><option value="<%= String(d).padStart(2, '0') %>"><%= d %></option><% } %>
                    </select>
                    <input type="number" id="birthday_year" inputmode="numeric" min="1900" max="<%= new Date().getFullYear() %>" placeholder="Year (optional)" aria-label="Birth year (optional)">
                </div>
                <p class="hint" id="birthday-hint">Year is optional. Other members see only the month and day &mdash; never the year.</p>
            </div>

            <div class="form-row">
                <span class="form-label" id="household-label">Household / Family</span>
                <p class="hint" id="household-hint">Please don't include details about anyone who hasn't agreed to be listed (for example, children).</p>
                <table class="household-table" id="householdTable" aria-labelledby="household-label" aria-describedby="household-hint">
                    <thead>
                        <tr>
                            <th scope="col">Name</th>
                            <th scope="col">Relationship</th>
                            <th scope="col">Birthday</th>
                            <th scope="col"><span class="sr-only">Actions</span></th>
                        </tr>
                    </thead>
                    <tbody id="householdRows">
                        <% (profile.household || []).forEach(function (person) { %>
                            <tr class="household-row">
                                <td data-cell="name"><%= person.name %></td>
                                <td data-cell="relationship"><%= person.relationship || '' %></td>
                                <td data-cell="birthday"><%= person.birthday || '' %></td>
                                <td><button type="button" class="link-button household-remove">Remove</button></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
                <p class="household-empty hint" id="householdEmpty" <%= (profile.household && profile.household.length > 0) ? 'hidden' : '' %>>No household members added yet.</p>
                <button type="button" class="btn-secondary" id="householdAddBtn" aria-haspopup="dialog">Add person</button>
            </div>

            <div class="household-modal" id="householdModal" role="dialog" aria-modal="true" aria-labelledby="household-modal-title" hidden>
                <div class="household-modal-content">
                    <h3 id="household-modal-title">Add a household member</h3>
                    <div class="form-row">
                        <label for="hm-name">Name</label>
                        <input type="text" id="hm-name" maxlength="80">
                    </div>
                    <div class="form-row">
                        <label for="hm-relationship">Relationship</label>
                        <input type="text" id="hm-relationship" maxlength="60" placeholder="e.g. Spouse, Child">
                    </div>
                    <div class="form-row">
                        <label for="hm-birthday">Birthday (optional)</label>
                        <input type="date" id="hm-birthday">
                    </div>
                    <p class="form-message error" id="householdModalError" role="alert"></p>
                    <div class="household-modal-actions">
                        <button type="button" class="btn-primary" id="householdModalAdd">Add</button>
                        <button type="button" class="btn-secondary" id="householdModalCancel">Cancel</button>
                    </div>
                </div>
            </div>

            <div class="form-row">
                <label for="bio">About Me</label>
                <textarea id="bio" name="bio" rows="4" maxlength="500"><%= profile.bio || '' %></textarea>
            </div>

            <div class="form-row">
                <span class="form-label" id="interests-label">Committees / Interests</span>
                <div class="tag-stack" id="interestsStack" role="list" aria-labelledby="interests-label" aria-describedby="interests-hint">
                    <% (profile.interests ? profile.interests.split(',') : []).forEach(function (raw) { const tag = raw.trim(); if (tag) { %>
                        <span class="tag-chip" role="listitem" data-tag="<%= tag %>"><span class="tag-chip-label"><%= tag %></span><button type="button" class="tag-chip-remove" aria-label="Remove <%= tag %>">&times;</button></span>
                    <% } }); %>
                </div>
                <input type="text" id="interestsEntry" maxlength="60" aria-describedby="interests-hint" placeholder="Type a committee and press Enter">
                <p class="hint" id="interests-hint">Type a committee or interest and press Enter to add it. Other members can search by these.</p>
            </div>

            <label class="checkbox-row directory-optin">
                <input type="checkbox" id="listed" name="listed" <%= profile.listed ? 'checked' : '' %>>
                <strong>List me in the member directory</strong>
            </label>
            <p class="hint" id="listed-hint">When on, the details above become visible to other logged-in members. Turn it off any time to leave the directory; your information is kept but hidden. Until it's on, the visibility options below stay disabled.</p>

            <fieldset class="settings-section visibility-fieldset" id="visibilityFieldset" aria-labelledby="visibility-heading">
                <legend id="visibility-heading"><h2>What others can see</h2></legend>
                <p class="hint">Your name and the items above are shown when you're listed. Choose which contact details to reveal:</p>
                <label class="checkbox-row">
                    <input type="checkbox" name="show_phone" <%= profile.show_phone ? 'checked' : '' %>>
                    Show my phone
                </label>
                <label class="checkbox-row">
                    <input type="checkbox" name="show_address" <%= profile.show_address ? 'checked' : '' %>>
                    Show my address
                </label>
                <label class="checkbox-row">
                    <input type="checkbox" name="show_birthday" <%= profile.show_birthday ? 'checked' : '' %>>
                    Show my birthday (month &amp; day)
                </label>
                <label class="checkbox-row">
                    <input type="checkbox" name="show_email" <%= profile.show_email ? 'checked' : '' %>>
                    Show my email (<%= profile.email %>)
                </label>
                <label class="checkbox-row">
                    <input type="checkbox" name="show_household" <%= profile.show_household ? 'checked' : '' %>>
                    Show my household / family
                </label>
                <label class="checkbox-row">
                    <input type="checkbox" name="household_consent" <%= profile.show_household ? 'checked' : '' %>>
                    I confirm I'm not sharing details about anyone who hasn't consented.
                </label>
            </fieldset>

            <button type="submit" class="btn-primary">Save Listing</button>
            <div class="form-message" id="directoryMessage" role="alert"></div>
        </form>
    </section>
</div>

<script src="/js/directory-listing.js" defer></script>
